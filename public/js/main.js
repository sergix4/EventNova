// public/js/main.js
// JavaScript del lado del cliente (se ejecuta en el navegador del usuario).
// Aqui va la logica de interaccion: validaciones de formularios, llamadas
// fetch() al backend, manejo de eventos de botones, etc.

document.addEventListener('DOMContentLoaded', () => {
  console.log('EventNova cargado correctamente.');
});
